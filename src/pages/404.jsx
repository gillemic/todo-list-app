import React from 'react';

const NotFoundStyle = {
    textAlign: "center",
    backgroundColor: "#fff",
    fontSize: "24px"
}

const NotFoundPage = () => {
    return (
        <div style={NotFoundStyle}>
            <h1>Page not found</h1>
        </div>
    );
}

export default NotFoundPage;